{
  "$comment": "Compiled from Rojo v7.7.0 by rojo-schema. Do not edit by hand.",
  "$defs": {
    "AmbiguousValue": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "string"
        },
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "number"
        },
        {
          "items": {
            "type": "number"
          },
          "maxItems": 2,
          "minItems": 2,
          "type": "array"
        },
        {
          "items": {
            "type": "number"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        },
        {
          "items": {
            "type": "number"
          },
          "maxItems": 4,
          "minItems": 4,
          "type": "array"
        },
        {
          "items": {
            "type": "number"
          },
          "maxItems": 12,
          "minItems": 12,
          "type": "array"
        },
        {
          "additionalProperties": {
            "$ref": "#/$defs/UnresolvedValue"
          },
          "type": "object"
        },
        {
          "type": "object"
        },
        {
          "type": "object"
        }
      ]
    },
    "UnresolvedValue": {
      "anyOf": [
        {
          "type": "object"
        },
        {
          "$ref": "#/$defs/AmbiguousValue"
        }
      ],
      "description": "A user-friendly version of `Variant` that supports specifying ambiguous values. Ambiguous values need a reflection database to be resolved to a usable value.\n\nThis type is used in Rojo projects and JSON models to make specifying the most common types of properties, like strings or vectors, much easier."
    }
  },
  "$id": "https://raw.githubusercontent.com/rbx-forge/rojo-schema/main/schema/meta.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "description": "Metadata applied to the instance produced by a neighbouring file or by the containing directory. Rojo reads `name.meta.json` next to `name.luau`, and `init.meta.json` inside a directory. `className` is only read from an `init.meta.json`; elsewhere Rojo ignores it.\n\nRepresents metadata in a sibling file with the same basename.\n\nAs an example, hello.meta.json next to hello.lua would allow assigning additional metadata to the instance resulting from hello.lua.",
  "properties": {
    "$schema": {
      "type": [
        "string",
        "null"
      ]
    },
    "attributes": {
      "additionalProperties": {
        "$ref": "#/$defs/UnresolvedValue"
      },
      "type": "object"
    },
    "className": {
      "type": [
        "string",
        "null"
      ]
    },
    "id": {
      "type": [
        "string",
        "null"
      ]
    },
    "ignoreUnknownInstances": {
      "type": [
        "boolean",
        "null"
      ]
    },
    "properties": {
      "additionalProperties": {
        "$ref": "#/$defs/UnresolvedValue"
      },
      "type": "object"
    }
  },
  "title": "Rojo meta file",
  "type": "object"
}
